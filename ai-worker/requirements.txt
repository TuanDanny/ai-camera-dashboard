ultralytics>=8.0.0
opencv-python-headless
paho-mqtt
pyyaml

# hailo_platform/picamera2 (can cho model.backend: hailo) KHONG nam trong
# file nay vi chi cai duoc qua apt (xem ghi chu trong config.yaml.example).
