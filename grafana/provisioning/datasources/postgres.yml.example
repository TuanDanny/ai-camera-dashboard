apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    database: shtp_traffic
    user: shtp
    secureJsonData:
      password: "changeme"
    jsonData:
      sslmode: disable
      postgresVersion: 15
      timescaledb: false
    isDefault: true
